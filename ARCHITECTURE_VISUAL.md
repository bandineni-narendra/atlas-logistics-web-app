# Generic Sheet Builder - Visual Architecture

## 📐 High-Level Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                           │
│                                                                   │
│  ┌──────────────────┐              ┌──────────────────┐         │
│  │  Air Freight     │              │  Ocean Freight   │         │
│  │  Sheet Page      │              │  Sheet Page      │         │
│  └────────┬─────────┘              └────────┬─────────┘         │
│           │                                  │                   │
│           │ Injects Config                  │ Injects Config    │
│           └──────────────┬──────────────────┘                   │
│                          │                                       │
└──────────────────────────┼───────────────────────────────────────┘
                           │
                           ▼
              ┌────────────────────────┐
              │   GENERIC CORE LAYER   │
              │                        │
              │  ┌──────────────────┐  │
              │  │  SheetBuilder    │  │
              │  │   Component      │  │
              │  └────────┬─────────┘  │
              │           │            │
              │  ┌────────▼─────────┐  │
              │  │  Sheet Manager   │  │
              │  │  (useSheetMgr)   │  │
              │  └────────┬─────────┘  │
              │           │            │
              │  ┌────────▼─────────┐  │
              │  │   Sheet Models   │  │
              │  │ Sheet│Column│Row │  │
              │  └──────────────────┘  │
              └────────────────────────┘
                  ↓         ↓         ↓
         ┌────────────┬────────────┬────────────┐
         │            │            │            │
    ┌────▼───┐  ┌────▼───┐  ┌────▼───┐  ┌────▼───┐
    │ Sheet  │  │ Table  │  │  Tabs  │  │ Cells  │
    │ Table  │  │  Row   │  │        │  │        │
    └────────┘  └────────┘  └────────┘  └────────┘
```

---

## 🔄 Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        DOMAIN LAYER                              │
│                                                                   │
│  Air Freight Domain              Ocean Freight Domain            │
│  ┌─────────────────┐             ┌─────────────────┐            │
│  │ AirRate Model   │             │ OceanRate Model │            │
│  │ - origin        │             │ - pol           │            │
│  │ - destination   │             │ - pod           │            │
│  │ - airline       │             │ - carrier       │            │
│  │ - rates         │             │ - containers    │            │
│  └────────┬────────┘             └────────┬────────┘            │
│           │                               │                      │
│  ┌────────▼────────┐             ┌────────▼────────┐            │
│  │ airColumns      │             │ oceanColumns    │            │
│  │ Configuration   │             │ Configuration   │            │
│  └────────┬────────┘             └────────┬────────┘            │
│           │                               │                      │
│           └───────────────┬───────────────┘                      │
│                           │                                      │
└───────────────────────────┼──────────────────────────────────────┘
                            │
                            │ Props (initialColumns)
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                         CORE LAYER                               │
│                  (Domain Agnostic)                               │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    SheetBuilder                           │   │
│  │  - Receives column config via props                      │   │
│  │  - Renders generic UI                                    │   │
│  │  - Emits data changes via onChange callback              │   │
│  └────────────────────┬─────────────────────────────────────┘   │
│                       │                                          │
│  ┌────────────────────▼─────────────────────────────────────┐   │
│  │              useSheetManager Hook                         │   │
│  │  - Manages multiple sheets                               │   │
│  │  - Add/remove sheets                                     │   │
│  │  - Track active sheet                                    │   │
│  └────────────────────┬─────────────────────────────────────┘   │
│                       │                                          │
│  ┌────────────────────▼─────────────────────────────────────┐   │
│  │                 Generic Models                            │   │
│  │                                                           │   │
│  │  Sheet {                  Row {                          │   │
│  │    id: string               id: string                   │   │
│  │    name: string             cells: {                     │   │
│  │    columns: Column[]          [columnId]: CellValue      │   │
│  │    rows: Row[]              }                            │   │
│  │  }                        }                              │   │
│  │                                                           │   │
│  │  Column {                 CellValue =                    │   │
│  │    id: string               string | number | null       │   │
│  │    label: string                                         │   │
│  │    type: ColumnType                                      │   │
│  │    required?: boolean                                    │   │
│  │  }                                                        │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
                            │
                            │ onChange callback
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    DOMAIN LAYER (Validation)                     │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Domain Page (e.g., CreateAirSheet)                      │   │
│  │  1. Receives raw sheet data                              │   │
│  │  2. Maps to domain model (AirRate)                       │   │
│  │  3. Validates business rules                             │   │
│  │  4. Exports/saves to API                                 │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

---

## 🧩 Component Hierarchy

```
SheetBuilder
│
├── SheetTabs (if multiSheet=true)
│   ├── Tab (Sheet 1)
│   ├── Tab (Sheet 2)
│   └── Add Sheet Button
│
└── SheetTable
    │
    ├── TableHeader
    │   ├── Row Number Column (#)
    │   ├── Column Header (with delete button)
    │   ├── Column Header (with delete button)
    │   └── ...
    │
    ├── TableRow (for each row)
    │   ├── Row Number Cell (with delete button)
    │   ├── TableCell (editable)
    │   ├── TableCell (editable)
    │   └── ...
    │
    ├── AddRowButton (➕)
    └── AddColumnButton (➕ Add Column)
```

---

## 📦 Folder Structure Tree

```
src/
│
├── core/                           ← GENERIC LAYER
│   ├── sheet-builder/
│   │   ├── components/
│   │   │   ├── SheetTabs/
│   │   │   │   ├── SheetTabs.tsx
│   │   │   │   └── index.ts
│   │   │   ├── SheetTable/
│   │   │   │   ├── SheetTable.tsx
│   │   │   │   └── index.ts
│   │   │   ├── TableHeader/
│   │   │   ├── TableRow/
│   │   │   ├── TableCell/
│   │   │   ├── AddRowButton/
│   │   │   ├── AddColumnButton/
│   │   │   └── index.ts
│   │   ├── hooks/
│   │   │   ├── useSheetManager.ts
│   │   │   ├── useSheetState.ts
│   │   │   └── index.ts
│   │   ├── models/
│   │   │   ├── Sheet.ts
│   │   │   ├── Column.ts
│   │   │   ├── Row.ts
│   │   │   ├── Cell.ts
│   │   │   └── index.ts
│   │   ├── types/
│   │   │   ├── ColumnType.ts
│   │   │   └── index.ts
│   │   ├── SheetBuilder.tsx
│   │   └── index.ts              ← Public API
│   └── index.ts
│
├── domains/                        ← DOMAIN LAYER
│   ├── air-freight/
│   │   ├── models/
│   │   │   ├── AirRate.ts        ← Domain Model
│   │   │   └── index.ts
│   │   ├── config/
│   │   │   ├── airColumns.ts     ← Column Config
│   │   │   └── index.ts
│   │   ├── pages/
│   │   │   └── CreateAirSheet.tsx ← Domain Page
│   │   └── index.ts
│   │
│   ├── ocean-freight/
│   │   ├── models/
│   │   │   ├── OceanRate.ts      ← Domain Model
│   │   │   └── index.ts
│   │   ├── config/
│   │   │   ├── oceanColumns.ts   ← Column Config
│   │   │   └── index.ts
│   │   ├── pages/
│   │   │   └── CreateOceanSheet.tsx ← Domain Page
│   │   └── index.ts
│   │
│   └── index.ts
│
└── app/                            ← NEXT.JS ROUTES
    ├── air-freight-sheet/
    │   └── page.tsx
    └── ocean-freight-sheet/
        └── page.tsx
```

---

## 🎯 Separation of Concerns

```
┌─────────────────────────────────────────────────────────────────┐
│                           CORE LAYER                             │
│                      (Domain Agnostic)                           │
│                                                                   │
│  ✅ What it DOES:                                                │
│     - Renders Excel-like UI                                      │
│     - Manages sheet/row/column state                             │
│     - Handles add/remove operations                              │
│     - Provides editable cells                                    │
│     - Emits data changes                                         │
│                                                                   │
│  ❌ What it DOESN'T do:                                          │
│     - Know about Air Freight, Ocean Freight, etc.                │
│     - Perform business validation                                │
│     - Contain domain-specific logic                              │
│     - Know field names like "airline" or "carrier"               │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                          DOMAIN LAYER                            │
│                        (Domain Specific)                         │
│                                                                   │
│  ✅ What it DOES:                                                │
│     - Define strict business models                              │
│     - Provide column configurations                              │
│     - Validate business rules                                    │
│     - Map sheet data to domain models                            │
│     - Handle API communication                                   │
│                                                                   │
│  ❌ What it DOESN'T do:                                          │
│     - Render UI components                                       │
│     - Manage sheet state                                         │
│     - Import core components                                     │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔗 Configuration Injection Pattern

```
Domain Page
    │
    │ 1. Defines columns
    │
    ▼
const airColumns: Column[] = [
  { id: 'origin', label: 'Origin', type: ColumnType.TEXT },
  { id: 'destination', label: 'Destination', type: ColumnType.TEXT },
  ...
];
    │
    │ 2. Injects into core
    │
    ▼
<SheetBuilder
  initialColumns={airColumns}  ← Configuration injection
  onChange={handleChange}
/>
    │
    │ 3. Core renders based on config
    │
    ▼
Generic SheetBuilder
    - Doesn't know these are "air freight" columns
    - Just sees: Column[], Row[]
    - Renders accordingly
    │
    │ 4. Data flows back
    │
    ▼
onChange callback receives Sheet[]
    │
    │ 5. Domain validates
    │
    ▼
Domain Page
    - Maps to AirRate model
    - Validates business rules
    - Exports/saves
```

---

## 🧪 Adding a New Domain (Step by Step)

```
Step 1: Create Folder Structure
  src/domains/my-domain/
  ├── models/
  ├── config/
  ├── pages/
  └── index.ts

Step 2: Define Model
  models/MyModel.ts
  └─→ Strict TypeScript interface

Step 3: Create Config
  config/myColumns.ts
  └─→ Column[] array using createColumn()

Step 4: Build Page
  pages/CreateMySheet.tsx
  └─→ Use <SheetBuilder initialColumns={myColumns} />

Step 5: Add Route
  app/my-domain-sheet/page.tsx
  └─→ Import and export domain page

Step 6: Test
  Navigate to /my-domain-sheet
  └─→ See your custom sheet builder!
```

---

## 📊 State Management Flow

```
User Action (e.g., Edit Cell)
    │
    ▼
TableCell onChange handler
    │
    ▼
SheetBuilder's updateSheet()
    │
    ▼
useSheetManager hook
    │
    ▼
Update sheets state
    │
    ▼
Re-render affected components
    │
    ▼
onChange callback fired
    │
    ▼
Domain Page receives updated Sheet[]
    │
    ▼
Domain Page can validate/export
```

---

## 🎨 UI Component Structure

```
┌────────────────────────────────────────────────────────────┐
│  SheetBuilder Component                                     │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  SheetTabs                                          │    │
│  │  [Sheet 1] [Sheet 2] [+ Add Sheet]                 │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  SheetTable                                         │    │
│  │                                                      │    │
│  │  ┌────────────────────────────────────────────┐    │    │
│  │  │ TableHeader                                 │    │    │
│  │  │  # │ Origin │ Dest │ Airline │ ...         │    │    │
│  │  └────────────────────────────────────────────┘    │    │
│  │                                                      │    │
│  │  ┌────────────────────────────────────────────┐    │    │
│  │  │ TableRow                                    │    │    │
│  │  │  1🗑│ [JFK] │ [LHR] │ [Emirates] │ ...     │    │    │
│  │  └────────────────────────────────────────────┘    │    │
│  │                                                      │    │
│  │  ┌────────────────────────────────────────────┐    │    │
│  │  │ TableRow                                    │    │    │
│  │  │  2🗑│ [LAX] │ [CDG] │ [Air France] │ ...   │    │    │
│  │  └────────────────────────────────────────────┘    │    │
│  │                                                      │    │
│  │  ➕ Add Row    ➕ Add Column                        │    │
│  │                                                      │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
└────────────────────────────────────────────────────────────┘
```

---

This visual guide helps understand how all the pieces fit together!
